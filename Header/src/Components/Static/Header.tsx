import React from "react";
import styled from "styled-components";

const Header = () => {
  return <HeaderCode>Header</HeaderCode>;
};

export default Header;

const HeaderCode = styled.div`
  display: flex;
  justify-content: center;
  padding-top: 50px;
`;
